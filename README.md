# GradleTest

A simple Java project managed with Gradle.

## Prerequisites

- Java Development Kit (JDK) 8 or higher
- Gradle (provided via wrapper)

## Build

To build the project, run:

```bash
./gradlew build
```

## Run

To run the application, use:

```bash
./gradlew run
```
